import Card from "../Card/Card";

const Profile = () => {
    return (
        <div>
            <div>Profile</div>
            <Card content={'Card in Profile'}/>
        </div>
    )
}

export default Profile;